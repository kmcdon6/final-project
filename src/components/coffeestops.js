import * as constants from "./constants.js"
import React, { useState, useRef, useEffect }from "react";

const coffeestops = e => {
    stops == constants.setDistance/constants.rangeValue
    console.log(constants.setDistance)
    return <div>Distance between Coffee: {stops}</div>
}